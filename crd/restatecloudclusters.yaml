apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: restatecloudclusters.restate.dev
spec:
  group: restate.dev
  names:
    kind: RestateCloudCluster
    plural: restatecloudclusters
    shortNames:
    - rcc
    singular: restatecloudcluster
  scope: Cluster
  versions:
  - additionalPrinterColumns: []
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for RestateCloudClusterSpec via `CustomResource`
        properties:
          spec:
            description: Represents the configuration of a Restate Cluster
            properties:
              authentication:
                description: Where to get credentials for communication with the Cloud cluster
                properties:
                  secret:
                    description: Configured a reference to a secret in the same namespace as the operator
                    properties:
                      key:
                        description: The key to read from the referenced Secret
                        type: string
                      name:
                        description: The name of the referenced secret. It must be in the same namespace as the operator.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - secret
                type: object
              environmentId:
                description: The environment ID of your cluster, which begins `env_`
                type: string
              region:
                description: The short region identifier of your cluster, eg `us`, `eu`.
                type: string
            required:
            - authentication
            - environmentId
            - region
            type: object
            x-kubernetes-validations: []
        required:
        - spec
        title: RestateCloudCluster
        type: object
    served: true
    storage: true
    subresources: {}
